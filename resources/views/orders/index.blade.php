@extends('layouts.app', ['title' => 'My Orders'])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h2 mb-0">{{ $management ? 'Order management' : 'My orders' }}</h1>@if ($management)<a href="{{ route('dashboard') }}">Back to dashboard</a>@endif</div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Order</th>@if ($management)<th>Customer</th><th>Pharmacy</th>@endif<th>Date</th><th>Status</th><th>Total</th><th></th></tr></thead><tbody>@forelse ($orders as $order)<tr><td>{{ $order->order_number }}</td>@if ($management)<td>{{ $order->user->name }}</td><td>{{ $order->pharmacy->name }}</td>@endif<td>{{ $order->created_at->format('d M Y') }}</td><td><span class="badge text-bg-primary">{{ ucfirst($order->status) }}</span></td><td>Rs {{ number_format((float) $order->total_amount, 2) }}</td><td><a href="{{ route($management ? 'manage.orders.show' : 'orders.show', $order) }}">View details</a></td></tr>@empty<tr><td class="text-center text-muted py-5" colspan="{{ $management ? 7 : 5 }}">No orders yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $orders->links() }}</div>
@endsection