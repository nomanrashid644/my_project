@extends('layouts.app', ['title' => 'Dashboard'])
@section('content')
<div class="page-heading"><p class="eyebrow mb-2">{{ str_replace('_', ' ', auth()->user()->role) }} workspace</p><h1 class="h2">Welcome, {{ auth()->user()->name }}</h1><p class="text-secondary mb-0">Everything you need for your CityCare role, in one place.</p></div>
<div class="row g-4"><div class="col-md-4"><div class="card h-100"><div class="card-body p-4"><div class="eyebrow mb-3">01 · Catalog</div><h2 class="h5">Medicine catalog</h2><p class="text-secondary">Browse availability and manage inventory.</p><a class="btn btn-sm btn-outline-primary" href="{{ route('medicines.index') }}">Open catalog</a></div></div></div><div class="col-md-4"><div class="card h-100"><div class="card-body p-4"><div class="eyebrow mb-3">02 · Workflow</div><h2 class="h5">Orders</h2>@if (in_array(auth()->user()->role, ['admin', 'pharmacy_staff'], true))<p class="text-secondary">Review and update pharmacy orders.</p><a class="btn btn-sm btn-outline-primary" href="{{ route('manage.orders.index') }}">Manage orders</a>@elseif (auth()->user()->role === 'customer')<p class="text-secondary">Review your placed orders.</p><a class="btn btn-sm btn-outline-primary" href="{{ route('orders.index') }}">View orders</a>@elseif (auth()->user()->role === 'rider')<p class="text-secondary">View assigned deliveries and update progress.</p><a class="btn btn-sm btn-outline-primary" href="{{ route('delivery.index') }}">My deliveries</a>@else<p class="text-secondary">Delivery workflow will be available in a later phase.</p>@endif</div></div></div>@if (auth()->user()->role === 'admin')<div class="col-md-4"><div class="card h-100"><div class="card-body p-4"><div class="eyebrow mb-3">03 · Control room</div><h2 class="h5">Administration</h2><p class="text-secondary">View operational summaries and reports.</p><a class="btn btn-sm btn-primary" href="{{ route('admin.dashboard') }}">Open admin dashboard</a></div></div></div>@else<div class="col-md-4"><div class="card h-100"><div class="card-body p-4"><div class="eyebrow mb-3">03 · Profile</div><h2 class="h5">Account</h2><p class="text-secondary">Role: {{ str_replace('_', ' ', auth()->user()->role) }}</p><a class="btn btn-sm btn-outline-primary" href="{{ route('profile.edit') }}">Manage profile</a></div></div></div>@endif</div>
@endsection