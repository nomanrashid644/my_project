@extends('layouts.app', ['title' => $medicine->medicine_name])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><a class="text-decoration-none" href="{{ route('medicines.index') }}">Back to catalog</a><h1 class="h2 mt-2">{{ $medicine->medicine_name }}</h1></div>@can('update', $medicine)<a class="btn btn-outline-primary" href="{{ route('medicines.edit', $medicine) }}">Edit medicine</a>@endcan</div>
<div class="card border-0 shadow-sm"><div class="card-body p-4"><div class="row g-4"><div class="col-md-7"><dl class="row mb-0"><dt class="col-sm-4">Category</dt><dd class="col-sm-8">{{ $medicine->category->name }}</dd><dt class="col-sm-4">Generic name</dt><dd class="col-sm-8">{{ $medicine->generic_name ?? 'Not listed' }}</dd><dt class="col-sm-4">Strength</dt><dd class="col-sm-8">{{ $medicine->strength ?? 'Not listed' }}</dd><dt class="col-sm-4">Dosage form</dt><dd class="col-sm-8">{{ $medicine->dosage_form ?? 'Not listed' }}</dd><dt class="col-sm-4">Description</dt><dd class="col-sm-8">{{ $medicine->description ?? 'No description available.' }}</dd></dl></div><div class="col-md-5 border-start"><p class="text-muted mb-1">Price</p><p class="display-6">Rs {{ number_format((float) $medicine->price, 2) }}</p><p class="mb-1">Stock quantity: <strong>{{ $medicine->stock_quantity }}</strong></p><span class="badge text-bg-{{ $medicine->stock_quantity > 0 ? 'success' : 'secondary' }}">{{ $medicine->availability }}</span>@if ($medicine->requires_prescription)<p class="alert alert-warning mt-4 mb-0">Prescription required before purchase.</p>@endif @auth @if (auth()->user()->role === 'customer' && $medicine->stock_quantity > 0)<form class="mt-4" method="POST" action="{{ route('cart.store', $medicine) }}">@csrf<div class="input-group"><input class="form-control" name="quantity" type="number" min="1" max="{{ $medicine->stock_quantity }}" value="1" required><button class="btn btn-primary" type="submit">Add to cart</button></div></form>@endif @endauth</div></div></div></div>
@can('delete', $medicine)<form class="mt-3" method="POST" action="{{ route('medicines.destroy', $medicine) }}" onsubmit="return confirm('Delete this medicine?')">@csrf @method('DELETE')<button class="btn btn-outline-danger" type="submit">Delete medicine</button></form>@endcan
@endsection