@extends('layouts.app', ['title' => 'Medicine Catalog'])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><p class="text-primary text-uppercase small fw-bold mb-1">CityCare Demo Pharmacy</p><h1 class="h2 mb-0">Medicine catalog</h1></div>@can('create', App\Models\Medicine::class)<a class="btn btn-primary" href="{{ route('medicines.create') }}">Add medicine</a>@endcan</div>
<form class="row g-2 mb-4" id="medicine-filter" method="GET" action="{{ route('medicines.index') }}"><div class="col-md-7"><label class="visually-hidden" for="q">Search</label><input class="form-control" id="q" name="q" value="{{ $search }}" placeholder="Search by medicine or generic name"></div><div class="col-md-3"><label class="visually-hidden" for="category">Category</label><select class="form-select" id="category" name="category" onchange="this.form.submit()"><option value="">All categories</option>@foreach ($categories as $category)<option value="{{ $category->id }}" @selected(request('category') == $category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-outline-primary w-100" type="submit">Search</button></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Medicine</th><th>Category</th><th>Price</th><th>Stock</th><th>Availability</th><th></th></tr></thead><tbody>@forelse ($medicines as $medicine)<tr><td><a class="fw-semibold text-decoration-none" href="{{ route('medicines.show', $medicine) }}">{{ $medicine->medicine_name }}</a><div class="small text-muted">{{ $medicine->generic_name ?? 'Generic name not listed' }}</div></td><td>{{ $medicine->category->name }}</td><td>Rs {{ number_format((float) $medicine->price, 2) }}</td><td>{{ $medicine->stock_quantity }}</td><td><span class="badge text-bg-{{ $medicine->stock_quantity > 0 ? 'success' : 'secondary' }}">{{ $medicine->availability }}</span></td><td class="text-end">@can('update', $medicine)<a class="btn btn-sm btn-outline-primary" href="{{ route('medicines.edit', $medicine) }}">Edit</a>@endcan</td></tr>@empty<tr><td class="text-center text-muted py-5" colspan="6">No medicines match your search.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $medicines->links() }}</div>
@endsection