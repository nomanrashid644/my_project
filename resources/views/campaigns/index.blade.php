@extends('layouts.app', ['title' => 'Campaign Management'])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h2 mb-0">Campaign management</h1><a class="btn btn-primary" href="{{ route('admin.campaigns.create') }}">Create campaign</a></div>@if (session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Campaign</th><th>Discount</th><th>Period</th><th>Usage</th><th>Status</th><th></th></tr></thead><tbody>@forelse ($campaigns as $campaign)<tr><td><strong>{{ $campaign->title }}</strong><div class="small text-muted">{{ $campaign->pharmacy->name }}</div></td><td>{{ $campaign->discount_type === 'percentage' ? $campaign->discount_value.'%' : 'Rs '.number_format((float) $campaign->discount_value, 2) }}</td><td>{{ $campaign->starts_at->format('d M Y') }} - {{ $campaign->ends_at->format('d M Y') }}</td><td>{{ $campaign->used_count }} / {{ $campaign->usage_limit ?? 'unlimited' }}</td><td><span class="badge text-bg-{{ $campaign->isCurrentlyValid() ? 'success' : 'secondary' }}">{{ $campaign->isCurrentlyValid() ? 'Active' : 'Inactive' }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.campaigns.edit', $campaign) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.campaigns.destroy', $campaign) }}" onsubmit="return confirm('Delete this campaign?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" type="submit">Delete</button></form></td></tr>@empty<tr><td class="text-center text-muted py-5" colspan="6">No campaigns created.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $campaigns->links() }}</div>
@endsection