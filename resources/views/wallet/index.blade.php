@extends('layouts.app', ['title' => 'My Wallet'])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><p class="text-primary text-uppercase small fw-bold mb-1">Customer account</p><h1 class="h2 mb-0">My wallet</h1></div><a href="{{ route('dashboard') }}">Back to dashboard</a></div><div class="card border-0 shadow-sm mb-4"><div class="card-body"><p class="text-muted mb-1">Available balance</p><p class="display-5 mb-0">Rs {{ number_format((float) $wallet->balance, 2) }}</p></div></div><h2 class="h4 mb-3">Transaction history</h2><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Date</th><th>Type</th><th>Description</th><th>Amount</th><th>Balance after</th></tr></thead><tbody>@forelse ($transactions as $transaction)<tr><td>{{ $transaction->created_at->format('d M Y H:i') }}</td><td>{{ ucfirst(str_replace('_', ' ', $transaction->type)) }}</td><td>{{ $transaction->description }}</td><td>{{ in_array($transaction->type, ['credit', 'refund'], true) ? '+' : '-' }} Rs {{ number_format((float) $transaction->amount, 2) }}</td><td>Rs {{ number_format((float) $transaction->balance_after, 2) }}</td></tr>@empty<tr><td class="text-center text-muted py-5" colspan="5">No wallet transactions yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $transactions->links() }}</div>
@endsection