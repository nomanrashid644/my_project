@extends('layouts.app', ['title' => 'Wallet Management'])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><p class="text-primary text-uppercase small fw-bold mb-1">Admin wallet management</p><h1 class="h2 mb-0">{{ $wallet->user?->name ?? $wallet->pharmacy?->name }}</h1></div><a href="{{ route('dashboard') }}">Back to dashboard</a></div>@if (session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if ($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach ($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="card border-0 shadow-sm mb-4"><div class="card-body"><p class="text-muted mb-1">Current balance</p><p class="display-5 mb-0">Rs {{ number_format((float) $wallet->balance, 2) }}</p></div></div><div class="card border-0 shadow-sm mb-4"><div class="card-body"><h2 class="h5">Record adjustment</h2><form class="row g-3" method="POST" action="{{ route('admin.wallets.adjust', $wallet) }}">@csrf<div class="col-md-3"><label class="form-label" for="type">Type</label><select class="form-select" id="type" name="type" required><option value="credit">Credit</option><option value="debit">Debit</option><option value="refund">Refund</option><option value="promotional_charge">Promotional charge</option></select></div><div class="col-md-3"><label class="form-label" for="amount">Amount</label><input class="form-control" id="amount" name="amount" type="number" min="0.01" step="0.01" required></div><div class="col-md-4"><label class="form-label" for="description">Description</label><input class="form-control" id="description" name="description" maxlength="255" required></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100" type="submit">Record</button></div></form></div></div><h2 class="h4 mb-3">Transaction history</h2><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Date</th><th>Type</th><th>Amount</th><th>Before</th><th>After</th><th>Created by</th></tr></thead><tbody>@forelse ($transactions as $transaction)<tr><td>{{ $transaction->created_at->format('d M Y H:i') }}</td><td>{{ ucfirst(str_replace('_', ' ', $transaction->type)) }}</td><td>Rs {{ number_format((float) $transaction->amount, 2) }}</td><td>Rs {{ number_format((float) $transaction->balance_before, 2) }}</td><td>Rs {{ number_format((float) $transaction->balance_after, 2) }}</td><td>{{ $transaction->creator?->name ?? 'System' }}</td></tr>@empty<tr><td class="text-center text-muted py-5" colspan="6">No wallet transactions yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $transactions->links() }}</div>
@endsection