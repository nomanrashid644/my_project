@extends('layouts.app', ['title' => 'Shopping Cart'])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h2 mb-0">Shopping cart</h1><a href="{{ route('medicines.index') }}">Continue browsing</a></div>
@if ($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach ($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
@if ($cart->items->isEmpty())<div class="card border-0 shadow-sm"><div class="card-body text-center py-5"><p class="text-muted">Your cart is empty.</p><a class="btn btn-primary" href="{{ route('medicines.index') }}">Browse medicines</a></div></div>@else
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Medicine</th><th>Unit price</th><th>Quantity</th><th>Subtotal</th><th></th></tr></thead><tbody>@foreach ($cart->items as $item)<tr><td>{{ $item->medicine->medicine_name }}</td><td>Rs {{ number_format((float) $item->unit_price, 2) }}</td><td><form class="d-flex gap-2" method="POST" action="{{ route('cart.update', $item) }}">@csrf @method('PUT')<input class="form-control form-control-sm" name="quantity" type="number" min="1" max="{{ $item->medicine->stock_quantity }}" value="{{ $item->quantity }}" required><button class="btn btn-sm btn-outline-primary" type="submit">Update</button></form></td><td>Rs {{ number_format((float) $item->unit_price * $item->quantity, 2) }}</td><td><form method="POST" action="{{ route('cart.destroy', $item) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" type="submit">Remove</button></form></td></tr>@endforeach</tbody></table></div><div class="card-body border-top d-flex justify-content-between align-items-center"><strong>Total: Rs {{ number_format($cart->subtotal, 2) }}</strong><a class="btn btn-primary" href="{{ route('checkout.create') }}">Proceed to checkout</a></div></div>
@endif
@endsection