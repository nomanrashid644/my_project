@extends('layouts.app', ['title' => 'User Management'])
@section('content')
<div class="page-heading d-flex justify-content-between align-items-center"><div><div class="eyebrow mb-2">Administration</div><h1 class="h2 mb-0">User management</h1><p class="text-secondary mt-2 mb-0">Control access for customers and your pharmacy team.</p></div><div class="d-flex gap-2"><a class="btn btn-primary" href="{{ route('admin.users.create') }}">Create team account</a><a class="btn btn-outline-secondary" href="{{ route('admin.dashboard') }}">Dashboard</a></div></div>@if (session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Joined</th><th>Status</th><th>Actions</th></tr></thead><tbody>@foreach ($users as $user)<tr><td class="fw-semibold">{{ $user->name }}</td><td>{{ $user->email }}</td><td><span class="badge text-bg-light">{{ str_replace('_', ' ', $user->role) }}</span></td><td>{{ $user->created_at->format('d M Y') }}</td><td><span class="badge text-bg-{{ $user->is_active ? 'success' : 'secondary' }}">{{ $user->is_active ? 'Active' : 'Inactive' }}</span></td><td class="text-end">@if ($user->id !== auth()->id())<form class="d-inline" method="POST" action="{{ route('admin.users.toggle', $user) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-primary" type="submit">{{ $user->is_active ? 'Deactivate' : 'Activate' }}</button></form>@if ($user->role !== 'admin')<button class="btn btn-sm btn-outline-secondary" data-bs-toggle="collapse" data-bs-target="#reset-{{ $user->id }}" type="button">Reset password</button><div class="collapse mt-2" id="reset-{{ $user->id }}"><form class="d-flex gap-1" method="POST" action="{{ route('admin.users.password', $user) }}">@csrf @method('PATCH')<input class="form-control form-control-sm" name="password" type="password" placeholder="New password" minlength="8" required><input class="form-control form-control-sm" name="password_confirmation" type="password" placeholder="Confirm" minlength="8" required><button class="btn btn-sm btn-primary" type="submit">Save</button></form></div>@endif @endif</td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $users->links() }}</div>
@endsection